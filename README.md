# HomeCleaning-BookingSystem

[查看需求文档](docs/Requirement.md);

要求：在开发用户端时，以vue-uniapp作为工作目录，制定plan后，将内容写入到该目录下的plan.MD文件；
     在开发后端时，以aspnet-core作为工作目录，制定plan后，将内容写入到该目录下的plan.MD文件；
     在开发管理端时，以vue-admin作为工作目录，制定plan后，将内容写入到该目录下的plan.MD文件；